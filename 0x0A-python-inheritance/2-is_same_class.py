#!/usr/bin/python3
"""checks if objects is an instance of a class"""


def is_same_class(obj, a_class):
    """Returns true if object is an instance of thr class, otherwise return false
    """
    if isinstance(obj, class):
        return True
    else:
        return False
